# Drop sprite art here

Transparent PNGs go in this folder. After dropping a file, register it in
`apps/mobile/src/design/spriteAssets.ts` (one line per file). Full asset list,
dimensions, and naming convention live in `docs/ART_SPEC.md`.

Nothing here is required to build — the game renders procedural placeholders
until real art is registered.
